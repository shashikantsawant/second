<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spoton.soliteapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainSqliteActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FormDetails"
             android:label="SQLite Application"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".FormContents"
            android:label="SQLite Application"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".DataListActivity"
            android:label="@string/title_activity_data_list"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <activity android:name=".InputId">
        </activity>
        <activity android:name=".FindPerticuler"></activity>
        <activity android:name=".DeleteActivity"></activity>

    </application>

</manifest>
